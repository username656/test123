const SERVER_PATH: string = 'http://zero-based-website-api.ey.devfactory.com';

export const environment: { [key: string]: string | boolean } = {
  serverPath: `${SERVER_PATH}`,
  apiPath: `${SERVER_PATH}/api`,
  production: false
};
